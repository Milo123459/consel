const chalk = require("chalk");
console.log(chalk`
{green Thanks for using consel.}
{blue To get started, run consel in the command line.}
{bold{blue{underline Got a new update? Just run consel -u}}}
`);
